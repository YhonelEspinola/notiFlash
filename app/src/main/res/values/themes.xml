<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotiFlash" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <!-- Fondo de ítem normal y seleccionado transparente -->
        <item name="itemRippleColor">@null</item>
        <item name="itemActiveIndicatorStyle">@style/CustomActiveIndicatorStyle</item>
    </style>
    <style name="CustomActiveIndicatorStyle">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="Theme.NotiFlash" parent="Base.Theme.NotiFlash" />


</resources>